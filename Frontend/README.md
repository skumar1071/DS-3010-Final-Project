How to run frontend:

Create virtual environment with python 3.10: python3.10 -m venv venv

Activate venv: source venv/bin/activate

Download requirements: pip install -r requirements.txt

Run streamlit app: streamlit run app.py